{
  "name": "7safe-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site institucional estatico da 7Safe MedTech.",
  "scripts": {
    "dev": "python -m http.server 8000",
    "start": "python -m http.server 8000",
    "build": "echo Static site - no build step required"
  }
}
